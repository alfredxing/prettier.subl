{
	"scripts": {
		"build": "yarn run tsc -p tsconfig.json",
		"watch": "yarn run tsc -w -p tsconfig.json"
	},
	"devDependencies": {
		"@types/node": "^14.14.37",
		"prettier": "^2.2.1",
		"typescript": "^4.2.4"
	}
}
